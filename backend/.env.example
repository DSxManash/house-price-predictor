# Backend config
# Comma-separated allowed frontend origins for CORS
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Max accepted CSV file size in MB for /batch_predict
MAX_UPLOAD_SIZE_MB=10
